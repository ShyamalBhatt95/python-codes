#write a Program to predict a given year is leap year or not

year = int(input("Enter a Year   : "))

if year % 4 == 0 and ( year % 100 or year % 400 == 0 ) : 
        print(f"{year} is a Leap Year")
else : 
    print(f"{year} is not a Leap Year")
